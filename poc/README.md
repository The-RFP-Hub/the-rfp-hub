# Design POC

Static mock of the proposed homepage (Tally direction) and inner pages. Deploy on Vercel with Root Directory = `poc`, no build command, output directory `.`.

Numbers come from `exports/latest.json` of 2026-09-15 and are hard-coded.
