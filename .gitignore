node_modules/
dist/
coverage/
*.tsbuildinfo
.turbo/
.DS_Store
*.log

# local env + API data exports (M2)
.env
.env.local
packages/api/exports/

# The TypeScript example installs from the npm registry as a consumer would (npm, not pnpm);
# its lockfile is a local artifact of running it, not part of the repo's dependency graph.
examples/typescript/package-lock.json
