---
"@the-rfp-hub/standard": minor
"rfphub-validate": minor
---

Ship spec v1.0.1: v1.0.0's contract with the standard's identity moved to `rfpsear.ch`. The schema `$id`, the context and the vocabulary now live under `https://rfpsear.ch`, and `specVersion` accepts `"1.0.0"` and `"1.0.1"`, so existing documents keep validating. `SPEC_VERSION` is `1.0.1`, and `schemas/v1.0.0/` still ships, unchanged.
