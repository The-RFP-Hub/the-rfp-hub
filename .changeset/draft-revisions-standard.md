---
"@the-rfp-hub/standard": major
---

Draft v1.0.0 revised in place (spec axis unchanged; the package API is what breaks here). Schema: organisation roles swapped (`operatingOrganizations` required and primary, `sponsoringOrganizations` optional), the six type blocks replaced by a required tagged `fundingDetails` union, `networks`/`tags`/`extensions` removed, `eligibility` now free text, `resourceLinks`→`additionalReferences`, `funding`→`fundingInfo`, `socialLinks` now an array of `{platform, url}` entries, `organization.slug` required, `organization.type`→`orgType`, `deadlines[].type`→`deadlineType`, and all date-time fields require UTC (trailing `Z`). Package exports: generated `Opportunity` reshaped (now carries a real `fundingDetails` discriminated union), `MonetaryAmount` removed, `SocialLinks`→`SocialLink`, `RegistryName` narrowed, `Timestamp`/`FundingDetails` added. Also: `serviceAgreement`, `milestones[]` and `programModel` promoted from provisional to stable (annotation-only); dual CJS/ESM type declarations, declared Node >=18 engine, JSON-LD context de-aliased, conformance suite expanded and renumbered. See ADRs 0004–0005 and the CHANGELOG field-mapping tables.
