---
"@the-rfp-hub/api": minor
"@the-rfp-hub/frontend": minor
---

Move to `rfpsear.ch`. The API stores and serves every opportunity as spec v1.0.1 (a migration re-declares existing rows) and keeps both the new and the pre-move apex reserved for the spec. The frontend redirects the pre-move hostnames to `rfpsear.ch`, except the spec's identifier paths, and declares new submissions as v1.0.1.
