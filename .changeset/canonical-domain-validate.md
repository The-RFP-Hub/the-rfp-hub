---
"rfphub-validate": patch
---

Pick up `@the-rfp-hub/standard`'s canonical identifiers. The validator declares no identifier of
its own and its API is unchanged; the embedded schema it compiles now carries its canonical
`$id`.
