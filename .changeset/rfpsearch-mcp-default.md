---
"@the-rfp-hub/mcp": patch
---

Default `RFPHUB_API_BASE` to `https://api.rfpsear.ch`, the project's new domain. `https://api.ethrfps.app` keeps serving the same API, so a pinned older release keeps working.
