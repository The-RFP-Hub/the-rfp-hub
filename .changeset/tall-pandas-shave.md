---
"@the-rfp-hub/frontend": patch
---

Stop the directory filters and the signed-in header overflowing narrow viewports, hold the filter bar's 44px touch targets on every pointer, and publish opportunity detail pages in the sitemap.
