# Generated by `next dev` / `next build`, and it points at `.next/types`, which is itself ignored.
# Committing a file whose first line says "do not edit" and whose reference target is untracked
# only creates a file that is wrong in a fresh clone. `pnpm typecheck` passes without it — that was
# checked, not assumed.
next-env.d.ts
