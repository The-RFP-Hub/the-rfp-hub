# RFP Hub UI QA — 2026-09-16

Preview: `https://the-rfp-hub-dashboard-git-brunodmsi-design-expl-karma-devs.vercel.app`
Branch: `brunodmsi/design-expl`

**Method note:** `page.screenshot()` in this ego-browser session hit a persistent
`CdpRequestTimeoutError: Page.captureScreenshot` on every page, including a bare
`example.com` control — a browser-infra issue in this session, not a site bug.
Findings below come from live DOM/accessibility inspection instead (computed
styles, bounding-box geometry, `scrollWidth`/`clientWidth`, image `naturalWidth`,
interaction receipts). The one real screenshot in this report is the share-card
PNG, pulled via `page.fetch()` since it's a static asset, not a screenshot:
`/private/tmp/claude-501/-Users-brunodemasi-orca-workspaces-rfp-hub-design-expl/97868010-4bb7-4f90-8515-d38232f20149/scratchpad/qa/ethonline-card.png`.
No visual screenshots of the app views themselves could be produced this pass.

---

## 1. Landing (`/`)

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| Both | No horizontal page scroll (`scrollWidth === clientWidth` at both 1440 and 390). Tallies are non-zero: 77 open opportunities, $119M, 70 orgs, 6 closing soon. Fine. | — | `.landing-tally*` | none |
| Both | Closing-soon strip (`ul.plain.landing-strip`) is a genuine `overflow-x:auto` container (1500px content in a 1176px box at 1440; similar at 390) and does not leak into page scroll. Fine. | — | `.landing-strip` | none |
| Both | Footer utility links are 20px or 14px tall: `About`, `Publishers`, `The Standard`, `API & data`, `GitHub`, `Privacy`, `Terms`, and the `Karma` / `Ethereum Foundation…` credit links (14px). All well under the 44px tap-target guideline; on 390px width `Karma`/`EF` link is only 14px tall with dense wrapped text. | P2 | `.shell-footer` links | Increase the footer link row's vertical padding/line-height to reach a 44px hit area, or wrap each link in a padded block even though the visible text stays small. |
| Both | `JSON` / `CSV` / `RSS` export links are 20px tall. | P2 | `.shell` open-data row | Same as above — pad the tap area. |
| 1440 | No images on the page (`totalImages: 0`) — org marks are rendered as text/CSS initials as intended, so no broken-image risk here. Fine. | — | — | none |

## 2. Directory (`/directory`, cards, `status=any`, `type=hackathon`)

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| Both | Table view already degrades to `display:block` rows with `data-label` attributes on mobile (a stacked-row pattern), not a horizontally-scrolling table. `scrollWidth === clientWidth` at 390. This is the right pattern. Fine. | — | `table`, `td[data-label]` | none |
| Both | `?status=any` correctly adds a **Status** column (`hasStatusColumn: true` only on that URL, false elsewhere). Fine. | — | table `thead` | none |
| Both | `?type=hackathon` correctly filters to 6 rows (verified count). Fine. | — | — | none |
| Both | Same small-tap-target pattern as landing: row title links inside the table are ~17px tall (text-line height, no vertical padding), footer links 14–20px, "Clear filters" / "Show only what is open" / "Show closed and upcoming too" toggle links are 20px tall. On a touch device these are thin, precision targets. | P1 | table row `<a>` cells; filter toggle links | Give the row anchor `padding-block` so the clickable area matches the row height (currently only the text glyph height is hittable), and bump filter-toggle links to at least 44px tap height (can keep 20px visual text with invisible padding). |
| Desktop | Table/Cards toggle (`<button>Table</button>` / `<button>Cards</button>`) works: clicking "Cards" changes the URL to `?view=cards` and swaps in `.opportunity-card` grid. Confirmed via interaction. Fine. | — | `.view-toggle` (buttons) | none |
| Desktop | Type pill "Hackathons 6" works: click updates URL to `?type=hackathon&view=cards` and re-renders exactly 6 cards. Fine. | — | type-pill buttons | none |
| Both | One row on `?status=any` ("Rocket Pool GMC Retrospective…") reports height 40px vs. the surrounding rows' 17px at both 1440 and 390 — likely a two-line title wrap that is fine content-wise, but worth a visual spot-check since it's the only row that wraps; no layout break detected (no overlap, no overflow). | P2 | `.directory-row` / table row for "Rocket Pool GMC Retrospective" | Confirm intentional wrap; if not, truncate long titles with ellipsis for row-height consistency. |

## 3. Listing page

`curated:starknet-bug-bounty` (the path named in the task) does **not** resolve — the app renders its own "We couldn't find this opportunity" not-found state instead of a 404 page-not-found, no console/network error, page still functions. This listing plainly doesn't exist in the current preview dataset.

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| Both | `curated:starknet-bug-bounty` 404s inside the app's own not-found template ("We couldn't find this opportunity… It may have moved, been merged, or no longer be available."). Not a broken page, but the URL given in the QA brief is stale — the dataset changed since brief was written. | P2 (data/doc drift, not a bug) | route `/opportunities/curated:starknet-bug-bounty` | If this URL is meant to stay a stable example, add a redirect/alias, or update whatever docs reference it. |
| Both | Used first real row instead: `curated:ethonline-2026`. Renders fully, `h1` = "ETHOnline 2026", no horizontal scroll at either width. | — | — | none |
| Desktop | Right-hand action column (`aside.opportunity-aside`) is `position: sticky; top: 16px` on desktop. Confirmed via computed style. Fine — matches spec. | — | `.opportunity-aside` | none |
| Mobile (390) | Same `aside.opportunity-aside` computes to `position: static` at 390px — it stacks under the content instead of staying sticky. Confirmed via computed style. Fine — matches spec. | — | `.opportunity-aside` | none |
| Both | "Share card" link (`<a class="button-primary action-primary">Share card</a>` inside `.action-card`) points to `/opportunities/curated%3Aethonline-2026/card.png`, a real `image/png` (fetched and verified 200, `content-type: image/png`). Opens the PNG as intended. Fine. | — | `.action-primary` "Share card" | none |
| Both | The page also embeds an `<img class="share-card-preview" loading="lazy" src=".../card.png">` further down (not visible above the fold). At initial paint it shows `naturalWidth: 0, complete: false` purely because it is native-lazy and hasn't entered the viewport — not an actual broken image (the underlying URL 200s). Worth a note only because a naive "is it broken" check flags it. | P2 | `.share-card-preview` | None required; if this needs to be scroll-triggered reliably, consider `loading="eager"` or an `IntersectionObserver` fetch since some crawlers/QA tools read `naturalWidth` without scrolling. |

## 4. Publishers (`/publishers`)

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| Both | Renders, `h1` = "Publishers", no horizontal scroll, no stuck loading state. Fine. | — | — | none |

## 5. How it works (`/how-it-works`)

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| Both | Renders, no horizontal scroll at either width. | — | — | none |
| Desktop | Expanded the "Who can do what" `<details>` section programmatically (native `<details>` toggle) — it opens to a permissions table without changing page width (`scrollWidth` stayed 1440). No overflow from the table. Fine. | — | `details` "Who can do what" | none |
| Both | 9 fold sections total ("The words this site uses", "Who can do what", "What verified means", "Why it works this way", "Why an organization decides in its own namespace", "Why four powers stay with Hub staff", "Why five submissions at a time", "The data is yours", "How decisions are made") — all start collapsed as expected. | — | — | none |

## 6. Privacy / Terms

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| Both | `/privacy` renders (`h1` = "Privacy"), no horizontal scroll. | — | — | none |
| Both | `/terms` renders (`h1` = "Terms of Use"), no horizontal scroll. | — | — | none |

## 7. Share card image (`/opportunities/curated:ethonline-2026/card.png`)

Used `curated:ethonline-2026` since `starknet-bug-bounty` 404s (see §3).

- Confirmed **1200×630 PNG**, `content-type: image/png`, HTTP 200.
- Visual check (saved locally and rendered): title, eyebrow ("Hackathon · ETHGlobal"), org name, Award/Next-deadline stat pair, tagline, and footer URL all sit in clear bands — **no text overlap**.
- One cosmetic nit: the footer URL prints the raw staging/preview hostname with a URL-encoded colon (`...vercel.app/opportunities/curated%3Aethonline-2026`), which is fine on a preview deploy but should be confirmed to render the production domain (and a readable, non-percent-encoded id) once this ships to the real domain.
- Screenshot saved: `/private/tmp/claude-501/-Users-brunodemasi-orca-workspaces-rfp-hub-design-expl/97868010-4bb7-4f90-8515-d38232f20149/scratchpad/qa/ethonline-card.png`

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| N/A (fixed 1200×630) | Card renders cleanly, no overlap. | — | — | none |
| N/A | Footer URL will show `%3A` (percent-encoded colon) in the id if the source id format (`curated:slug`) isn't re-encoded for display. | P2 | share-card footer URL text | Decode `%3A`→`:` (or drop the scheme prefix) before drawing the footer URL string onto the card. |

---

## Top 10 issues, ranked

1. **P1 — Directory row/footer links have sub-44px tap targets on touch.** Row title `<a>` cells and filter-toggle links (`Clear filters`, `Show only what is open`, `Show closed and upcoming too`) hit only their text-line height (~17–20px). Add vertical padding so the clickable area, not just the glyph box, reaches 44px. (`/directory`, both `table` and `cards` views)
2. **P2 — Footer utility/credit links (`About`, `Privacy`, `Terms`, `Karma`, EF credit) are 14–20px tall** site-wide (landing, directory, how-it-works). Same fix as #1, lower priority since it's a low-traffic footer.
3. **P2 — `curated:starknet-bug-bounty` referenced in this QA brief 404s** in the current preview dataset (app shows its own not-found state gracefully, but the example URL is stale). Worth confirming whether that listing was meant to still exist, or updating docs/fixtures that reference it.
4. **P2 — Share-card footer URL shows a raw percent-encoded colon** (`curated%3Aethonline-2026`) instead of a clean id — minor polish before this asset gets shared externally.
5. **P2 — One directory row ("Rocket Pool GMC Retrospective…") wraps to 40px height** while sibling rows are 17px — confirm this is intentional wrapping, not a truncation bug.
6. **P2 — `.share-card-preview` `<img>` is `loading="lazy"`** so any naive "is this image broken" check (including automated QA) will flag `naturalWidth: 0` on initial load even though the asset is a valid 200; not a real bug, but worth knowing if this preview image feeds any automated content check.
7. **Confirmed working, no issue** — landing tallies are non-zero and update: 77 opportunities / $119M / 70 orgs / 6 closing soon.
8. **Confirmed working, no issue** — closing-soon strip scrolls horizontally in its own container; page body never scrolls horizontally at 1440 or 390 on any page checked.
9. **Confirmed working, no issue** — directory table converts to a stacked `data-label` layout at 390px instead of a horizontally-scrolling table; Table/Cards toggle and type-pill filters both update the URL and re-render.
10. **Confirmed working, no issue** — listing page action column is `position: sticky` on desktop and `position: static` (stacked) on mobile, exactly as specified; "Share card" links to a valid 1200×630 PNG with no text overlap.

### Tooling caveat
Full-page and viewport screenshots could not be captured this session (`CdpRequestTimeoutError` on every `page.screenshot()` call, including a blank `example.com` control tab) — this looks like an ego-browser/session infra issue, not a site defect. All findings above are backed by live DOM geometry, computed styles, and interaction receipts rather than visual screenshots; only the share-card PNG (a real static asset, not a screenshot) could be captured and eyeballed. A follow-up pass with working screenshot capture is recommended to catch purely visual issues (color/contrast, subtle misalignment, font rendering) that DOM inspection can't see.
