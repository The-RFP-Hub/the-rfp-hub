# RFP Hub — Visual/Layout QA — 2026-09-16

Base URL: `https://the-rfp-hub-dashboard-git-brunodmsi-design-expl-karma-devs.vercel.app`
Target commit: `b7d26ad` ("frontend: table-only directory, leaner listing page, rules that open")

**Deployment freshness check:** passed on first load — no "Table / Cards" toggle
on `/directory`, no "Share card" link on the listing page. Preview was NOT stale;
no reload/wait loop was needed.

**Screenshot note:** `page.screenshot()` failed with `CdpRequestTimeoutError:
Page.captureScreenshot` on every attempt, including a retry at a smaller
390×600 viewport with `fullPage: false`. Per the task instructions, the rest of
this QA pass was done with DOM geometry (`getBoundingClientRect`, computed
styles, `scrollWidth`/`clientWidth`, `naturalWidth`, snapshot trees) instead of
screenshots. No screenshots were produced; the `qa2/` scratchpad directory
contains one artifact instead: the fetched OG image (see §7).

Method: ego-browser (CDP) driving one real Chromium tab, at 1440×900 and
390×844 (mobile, `mobile:true` device metrics). Read-only throughout — no
forms submitted, no login, no "Apply" clicks.

---

## 1. `/` (landing)

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| 1440 & 390 | Four tally rows ("77 open opportunities", "$119M in maximum awards", "70 organizations", "6 closing in 30 days") all share one left edge (793px desktop, 16px mobile) with one caption line each, as specified. Fine — no fix needed. | — | Hero stats block | — |
| 1440 | The six "Closing soon" cards (`.opportunity-card`) all measure exactly 175×240px — heights match. Fine. | — | Closing-soon grid | — |
| 1440 & 390 | Type tiles read "Grants / Hackathons / Bounties / RFPs" each with a count (19/6/44/5) and a one-line caption, matching spec. Fine. | — | "Browse by type" section | — |
| 1440 & 390 | No horizontal scroll (`scrollWidth === clientWidth` at both widths). Fine. | — | Whole page | — |
| 390 | Gutters measured at exactly 16px on header, hero section, H1, and footer — meets the 16px minimum. Fine. | — | All edge-to-edge containers | — |
| Both | No `<img>` elements exist on the landing page (icons/marks are inline SVG or text initials), so there was nothing to check for broken images here. | — | — | — |

No P0/P1 issues found on landing.

## 2. `/directory`, `/directory?status=any`, `/directory?type=hackathon`

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| 1440 | Table headers on `/directory` (default, `status` unset) are exactly `Type, Opportunity, Organization, Award, Closes` — no Status column, correct. | — | Directory table | — |
| 1440 | Table headers on `/directory?status=any` are `Type, Opportunity, Organization, Award, Status, Closes` — Status column appears only here, correct. | — | Directory table | — |
| 1440 | Clicking the "Hackathons" pill navigated to `/directory?type=hackathon` and the table re-rendered with exactly 6 rows, all `Type = Hackathon`. Filtering works correctly and updates the URL. | — | Type pill group | — |
| 390 | Table collapses to a stacked, labeled layout: each `tbody tr` becomes `display: grid` and each cell carries a `data-label` (Type / Organization / Award / Closes; the Opportunity cell has no explicit label but is the unlabeled first-position text row, which reads fine without one). Table width is 358px = 390 − 16 − 16, gutters correct. | — | Directory table, mobile | — |
| 390 | Type-pill tap targets ("All 77", "Grants 19", "Hackathons 6", "Bounties 44", "RFPs 5") all measure 44px tall — meets the 44px minimum. Fine. | — | Pill row, mobile | — |
| 1440 | Initial page load briefly shows an "Loading the directory…" output element before the table hydrates (resolved within ~1–2s in testing, well under the 10s budget). Not a bug, just noted. | P2 | Directory, initial load | If this is visible longer on colder loads/slower networks, consider a skeleton table instead of a spinner+text to reduce perceived shift when 20 rows pop in at once. |

No P0/P1 issues found on the directory pages.

## 3. First listing (`/opportunities/curated:ethonline-2026`, ETHOnline 2026)

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| 1440 | Breadcrumb (Directory / Hackathon / ETHGlobal), "ET" org-initials mark beside the H1, and a three-fact strip (Award / Next deadline / Applications) all present in that order, matching spec. | — | Listing header | — |
| 1440 | Right column contains, in order: Apply button ("Apply on ETHGlobal's site"), "Program site" link, "Copy link" button, indexed/updated dates + JSON link, and a folded "This is my program — claim it" `<details>` control. Matches spec exactly. | — | Right column | — |
| 1440 & 390 | Confirmed no dates card and no share-card preview anywhere on the page (`/Key dates/` and `/Share card/` both absent from page text). | — | — | — |
| 390 | Right column stacks under the main content: the Apply button's `top` is 3321px vs. the About-this-opportunity H2 at 652px — well below the body copy, confirming vertical stacking rather than a side rail. Fine. | — | Right column, mobile | — |
| 390 | "Program site" link measures 26px tall (below the 44px minimum tap target). | P2 | Right column, listing page | Increase the link's padding/line-height to reach 44px, or wrap it in a larger tap area, matching "Copy link" (44px) and the Apply button (44px) on the same rail. |
| 390 | The "This is my program — claim it" `<summary>` measures 23px tall (below the 44px minimum). | P2 | Folded claim control, listing page | Same fix — pad the `<summary>` row to 44px min-height so it's comfortably tappable, especially since it's a functional trigger (opens claim flow), not decorative text. |

## 4. `/publishers`

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| Both | Page renders correctly: verified publisher card (Ethereum Foundation) with namespace, description placeholder, external link, verified date, and a "View this publisher's listings" action; below it a ranked "Listed" table of 67 organizations by open-listing count. No overflow, no broken layout. | — | — | — |
| Both | A React hydration error was thrown on load: `Error: Minified React error #418` (hydration mismatch — server/client HTML mismatch), caught via `Runtime.exceptionThrown`. The page still renders visibly correct content, so this is a silent hydration failure rather than a visible break, but it indicates the server-rendered HTML doesn't match the client render for something on this page (commonly caused by non-deterministic content — e.g. a locale/timezone-formatted date, or `Math.random()`/`Date.now()` in render). | **P1** | `/publishers` (also seen on `/how-it-works` and `/terms`, see §5–6) | Find the source of the mismatch (check any `Date`-formatted verified/updated timestamps, locale-dependent number formatting, or client-only APIs called during SSR) and either move it into `useEffect`/a client-only wrapper or make the SSR output match the client's first paint exactly. Not blocking visually today, but hydration errors can cause interactive bugs (lost event handlers) on the affected subtree and should not ship to production. |

## 5. `/how-it-works`

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| 1440 | Clicking "Who can do what" in the "Read the rules" link row navigates to `#rule-matrix`, the corresponding `<details>` opens (`open: false → true`), and the page scrolls so the target's top is ≈0 (viewport-aligned), `scrollY` moved to 1123px. Works as specified. | — | Read-the-rules link row | — |
| 1440 | Clicking a fold `<summary>` directly (tested "The words this site uses") also toggles it open independently. Works as specified. | — | Fold list | — |
| Both | Same React hydration error (`#418`) observed as on `/publishers`. See §4 for detail/fix; not repeated as a separate P1 line, same root cause likely affects this route too. | P1 | `/how-it-works` | See §4. |

## 6. `/privacy`, `/terms`

| Width | Finding | Severity | Where | Suggested fix |
|---|---|---|---|---|
| Both | `/privacy` renders (H1 "Privacy", ~6.4k characters of body text, no overflow). Fine. | — | — | — |
| Both | `/terms` renders (H1 "Terms of Use", ~2.5k characters of body text, no overflow). No hydration exception was seen on `/privacy`, but one was seen on `/terms` (same `#418` error as §4/§5). | P1 | `/terms` | See §4 fix suggestion. |

---

## Top 10, ranked across the site

1. **[P1] React hydration error (#418) on `/publishers`, `/how-it-works`, `/terms`.** Silent today but a real SSR/client mismatch that risks losing interactivity on the affected subtree; worth root-causing before this design lands on `main`. (§4, §5, §6)
2. **[P2] "Program site" link is a 26px-tall tap target on the mobile listing page**, below the 44px minimum next to two 44px siblings (Apply, Copy link). (§3)
3. **[P2] "This is my program — claim it" fold summary is 23px tall on mobile** — same tap-target issue, and it's a functional trigger, not just copy. (§3)
4. **[P2] Directory shows a transient "Loading the directory…" state on first paint** before the table hydrates; resolved quickly in this test but worth a skeleton table for perceived-performance polish. (§2)
5. Landing tally rows: verified aligned on one left edge, one caption each, at both widths — **confirmed fine**, no action.
6. Closing-soon cards: verified uniform 175×240px height across all six — **confirmed fine**, no action.
7. Type tiles: verified "Grants / Hackathons / Bounties / RFPs" with counts at both widths — **confirmed fine**, no action.
8. Directory table→stacked-rows on mobile: verified `data-label` stacking and correct 16px gutters — **confirmed fine**, no action.
9. Listing page: verified breadcrumb, org-initials mark, three-fact strip, right-column contents, mobile stacking, and absence of dates/share cards — **confirmed fine**, no action.
10. No horizontal scroll, no broken images, and no 404 internal links were found anywhere in the pages tested — **confirmed fine**, no action.

No P0s were found in this pass.

---

## 7. Head metadata

| Page | `<title>` | `meta[name=description]` | `og:title` | `og:description` | `og:image` |
|---|---|---|---|---|---|
| `/` | `RFP Hub: an open index of Ethereum funding` | "An open index of funding opportunities under one standard. Read it without an account. Publishers can sign in to submit and maintain listings, check their traffic, and run their review queues." | `RFP Hub: an open index of Ethereum funding` | (same as description) | `https://…/card.png` |
| `/directory` | `Directory \| RFP Hub` | *same generic description as `/`* | `Directory \| RFP Hub` | *same generic description as `/`* | `https://…/card.png` (same site-wide image as `/`) |
| `/opportunities/curated:ethonline-2026` | `ETHOnline 2026 \| RFP Hub` | "ETHGlobal's flagship async online Ethereum hackathon, open worldwide, running September 4-16, 2026." | `ETHOnline 2026` | (same as description, listing-specific) | `https://…/opportunities/curated%3Aethonline-2026/card.png` (per-listing image) |

**Note (P2, not scored above since it's metadata-only):** `/directory`'s
`meta[description]` and `og:description` are identical to the homepage's
generic copy rather than directory-specific text (e.g. mentioning "77 open
opportunities"). Low priority, but a missed opportunity for a more useful
social-share preview and search snippet.

**OG image verification:** fetched the listing's `og:image`
(`…/opportunities/curated%3Aethonline-2026/card.png`) via `page.fetch()` —
`200 OK`, `content-type: image/png`. Saved to
`/private/tmp/claude-501/-Users-brunodemasi-orca-workspaces-rfp-hub-design-expl/97868010-4bb7-4f90-8515-d38232f20149/scratchpad/qa2/listing-card.png`
and confirmed via PNG header parsing: **1200×630, 8-bit RGBA** — matches the
required dimensions exactly.
