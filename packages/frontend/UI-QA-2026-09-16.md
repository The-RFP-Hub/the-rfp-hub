# RFP Hub UI QA — 2026-09-16

Base: `https://the-rfp-hub-dashboard-git-brunodmsi-design-expl-karma-devs.vercel.app`
Freshness gate: passed on first check (`/this-route-does-not-exist` rendered inside app shell with "RFP Hub" header and "There is nothing at this address." heading).

Method note: given prior `CdpRequestTimeoutError` failures with screenshots in this environment, this pass used DOM geometry (`getBoundingClientRect`, `scrollWidth`/`clientWidth`), `innerText` inspection, and CDP `Runtime`/`Log` event capture instead of screenshots. No screenshot was attempted this round.

## Fixes verified

| # | Item | Pass/Fail | Evidence |
|---|------|-----------|----------|
| 1 | `/this-route-does-not-exist` shell + heading + "Open the directory" link | PASS | 1440px and 390px: `h1` = "There is nothing at this address.", header contains "RFP Hub", link text "Open the directory" → `/directory`. No horizontal scroll either width. |
| 2 | `/organisations`, `/organisations/acme` → 308 to `/organizations…` | PASS | `curl -I`: both return `308` with `Location` on the `/organizations…` path. Navigation confirmed final URL lands on `/organizations` and `/organizations/acme`. |
| 3 | Signed-out gate title is `<h1>` on `/dashboard`, `/listings`, `/account`, `/keys`, `/review`, `/admin` | PASS | All six render exactly one `h1`: "Sign in to view your listings' traffic." (dashboard), "Sign in to manage your listings." (listings), "Sign in to view your account." (account), "Sign in to manage API keys." (keys), "Sign in with a Hub reviewer account." (review), "Sign in with a Hub administrator account." (admin). Note: dashboard/listings/account show a transient "restoring session…" state before the gate resolves — took up to ~10–15s to settle in this preview; worth checking real latency isn't this slow in prod. |
| 4 | `/directory?page=99` summary omits "page 99 of"; past-the-end message + "Back to page 1" | PASS | Body shows "Showing 77 open opportunities · closing soonest" (no "page 99 of" anywhere) and "Page 99 is past the end of this result." with a "Back to page 1" link → `/directory`. |
| 5 | Listing page: one muted "Type by Organization · ecosystems" line, no type chip/ecosystem pills, status badge only if not open, action-column ownership line, one-line apply button | PASS | `curated:ethonline-2026` (1st directory row): "Hackathon by ETHGlobal · Ethereum" as one line, no separate chips found nearby. `curated:rocket-pool-gmc-retrospective-awards-round-40`: "Grant by Rocket Pool Grants Management Committee (GMC) · Ethereum, Rocket Pool", one line. Both open listings show no status badge (consistent — badge only for non-open). Both show "Listed from public sources, not yet claimed" above the index dates. Apply CTA is a single 44px-tall one-line button ("Apply on the program's site" / "Apply on ETHGlobal's site"); a secondary plain-text raw-URL link also renders below it, which is expected reference text, not the CTA. Verified at 1440 and 390px, no horizontal scroll either. |
| 6 | `/` — tally, doors, closing-soon, type tiles | PASS | "77 open opportunities", "$119M in maximum awards", "70 organizations", "6 closing in the next 30 days" all present; "I'm looking for funding →" / "I publish a program →" doors present; Closing soon list of 6 cards; Browse by type tiles (19 Grants / 6 Hackathons / 44 Bounties / 5 RFPs) present. No regressions observed in text content. |
| 7 | `/publishers`, `/how-it-works` rules fold, `/privacy`, `/terms` — lede ≤2 sentences, Effective line | PARTIAL | Privacy lede = exactly 2 sentences, "Effective 31 August 2026." directly under it. Terms lede = exactly 2 sentences, "Effective 24 August 2026." directly under it. Both PASS. `/how-it-works`: the "Roles / Namespaces / Verification / …" rule section (`section.rule`) already renders with class `rule rule-open` **on first load**, before any click, in a fresh page/session. No `<a>` or `<button>` element matches "Read the rules" text — it appears to be static copy, not a live toggle control, so the "click it and confirm the fold opens" check could not be exercised: there is nothing to click, and the fold is already open by default. This may be intentional (progressive-disclosure copy repurposed as a static section head) but does not match the described interaction — flag for confirmation. |
| 8 | Console errors / CSP violations | PASS (clean) | Checked 9 pages (`/`, `/directory`, listing page, `/publishers`, `/how-it-works`, `/privacy`, `/terms`, `/dashboard`, `/directory?page=99`) via CDP `Runtime`/`Log` events: zero console errors, zero CSP violation log entries on any of them. |

## Per-route findings

| Width | Route | Finding | Severity | Where | Fix |
|-------|-------|---------|----------|-------|-----|
| 1440/390 | `/this-route-does-not-exist` | fine | — | — | — |
| 1440/390 | `/organizations`, `/organizations/acme` | fine | — | — | — |
| 1440/390 | `/dashboard`, `/listings`, `/account` | Gate content only resolves after a "restoring session…" / "Loading your session…" interstitial that took several seconds in this preview before the `h1` appeared | P3 | signed-out session-restore path | Verify real-world timing isn't this slow, or show the gate title immediately and swap in the CTA once session state resolves, instead of blocking the heading on it |
| 1440/390 | `/keys`, `/review`, `/admin` | fine, gate `h1` renders immediately | — | — | — |
| 1440/390 | `/directory?page=99` | fine | — | — | — |
| 1440/390 | listing pages (`ethonline-2026`, `rocket-pool-gmc-retrospective-awards-round-40`) | fine | — | — | — |
| 1440/390 | `/` | fine | — | — | — |
| 1440/390 | `/publishers` | fine | — | — | — |
| 1440/390 | `/how-it-works` | "Read the rules" is not an interactive element; the rules section is open by default on load, so the described click-to-expand behavior does not exist as spec'd | P2 | `/how-it-works`, `section.rule.rule-open` | Either wire "Read the rules" as a real toggle control (collapsed by default) or update the spec/copy — currently it reads as a link/CTA but is static text with the fold pre-opened |
| 1440/390 | `/privacy`, `/terms` | fine | — | — | — |

No horizontal page scroll, clipped text, overlaps, broken images, or stuck loading states were observed on any of the pages checked, at either width. No console errors or CSP violations surfaced.

## Ranked top-10

1. **P2** — `/how-it-works`: "Read the rules" is not a real link/button and the rules fold is already expanded on load; the click-to-open behavior in the spec doesn't exist to verify. Confirm intended behavior and fix the control or the copy.
2. **P3** — `/dashboard`, `/listings`, `/account`: gated pages pass a session-restore interstitial before the `h1` renders, adding a multi-second delay before the required heading is visible; worth a latency check outside this preview.
3. No other defects found this round — all remaining fix items (1, 2, 4, 5, 6, privacy/terms lede, console/CSP) verified clean at both widths.
4–10. (none — sweep came back clean beyond items 1–2 above)
